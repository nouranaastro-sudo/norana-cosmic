import React,{useEffect,useState} from 'react';import{createRoot}from'react-dom/client';import{supabase}from'./lib/supabaseClient';import{initAnalytics,trackEvent}from'./lib/analytics';import'./style.css';
const services=['تحليل الأسماء والأرقام','الخريطة الفلكية','التوافق والعلاقات','التاروت','تفسير الأحلام','قراءة الكف','قراءة الفنجان','الأحجار ورمز الحظ'];
function App(){const[user,setUser]=useState(null);const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');useEffect(()=>{initAnalytics();supabase.auth.getSession().then(({data})=>setUser(data.session?.user||null));const{data:{subscription}}=supabase.auth.onAuthStateChange((_e,s)=>setUser(s?.user||null));return()=>subscription.unsubscribe()},[]);
async function signUp(){setMsg('');const{error}=await supabase.auth.signUp({email,password,options:{emailRedirectTo:window.location.origin}});if(error)return setMsg(error.message);trackEvent('signup');setMsg('تحققي من بريدك لتأكيد الحساب.');}
async function login(){setMsg('');const{error}=await supabase.auth.signInWithPassword({email,password});if(error)return setMsg(error.message);trackEvent('login');setMsg('تم تسجيل الدخول.');}
async function logout(){await supabase.auth.signOut();setMsg('تم تسجيل الخروج.');}
return <main><header><div className="mark">✦</div><h1>NORANA</h1><p>Cosmic Insight · نورانا الكونية</p></header><section className="hero"><h2>رحلتك نحو فهم أعمق لنفسك وعلاقاتك</h2><p>قراءات شخصية وروحانية للاستبصار والترفيه، بالعربية والإنجليزية.</p><button onClick={()=>{trackEvent('premium_click');document.querySelector('#account').scrollIntoView({behavior:'smooth'})}}>Premium · $1</button></section><section><h2>الخدمات</h2><div className="grid">{services.map(s=><button className="card" key={s} onClick={()=>trackEvent('service_click',{service_name:s})}><span>✧</span>{s}</button>)}</div></section><section id="account" className="account"><h2>حساب NORANA</h2>{user?<><p>تم تسجيل الدخول</p><button onClick={logout}>تسجيل الخروج</button></>:<><input type="email" placeholder="البريد الإلكتروني" value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" placeholder="كلمة المرور" value={password} onChange={e=>setPassword(e.target.value)}/><div className="actions"><button onClick={login}>دخول</button><button onClick={signUp}>إنشاء حساب</button></div></>}{msg&&<p className="msg">{msg}</p>}</section><section className="consult"><h2>استشارة سريعة</h2><p>10 دقائق · $5</p><button onClick={()=>trackEvent('consultation_click')}>طلب استشارة</button></section><footer>© 2026 NORANA · All Rights Reserved.<br/><small>المحتوى الروحاني للترفيه والاستبصار الشخصي، وليس نصيحة طبية أو قانونية أو مالية.</small></footer></main>};createRoot(document.getElementById('root')).render(<App/>);